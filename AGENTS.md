# Contributor Guidelines

- Prefer minimal, fast unit tests; keep tensor shapes small so JAX/Flax setups stay lightweight.
- Run `pytest -q` before sending changes.
- Keep new documentation concise and co-located with the relevant code.
